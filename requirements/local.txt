-r production.txt

psycopg==3.2.9

# Testing
# ------------------------------------------------------------------------------
pytest==8.4.1
pytest-django==4.11.1
pytest-cov==6.2.1
model-bakery==1.20.5
Faker==37.5.3

# Code quality
# ------------------------------------------------------------------------------
ruff==0.12.7
djlint==1.36.4

# Windows compatibility and i18n helper
# ------------------------------------------------------------------------------
# Provide libmagic on Windows via python-magic-bin (import as `magic`).
python-magic-bin==0.4.14; platform_system == "Windows"
# Compile .po -> .mo without system gettext using polib
polib==1.2.0
